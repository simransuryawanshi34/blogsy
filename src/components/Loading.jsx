import { Loader } from ".";

const Loading = () => {
  return (
    <div className="max-w my-auto relative flex flex-col items-center justify-center">
      <Loader />
    </div>
  );
};

export default Loading;
